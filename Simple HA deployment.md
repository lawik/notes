Load balance using haproxy. 1-2 nodes.
DNS round robin probably to balance then probably.

Any kind of compute node. Probably VPS or dedicated.

Managed Postgres, HA pricing. Primarily want to try Upcloud.